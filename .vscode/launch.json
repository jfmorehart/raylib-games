{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug savo",
      "program": "${workspaceFolder}/savo",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build savo (debug)"
    }
  ]
}
